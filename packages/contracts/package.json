{
  "name": "contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network sapphireTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network sapphireMainnet",
    "verify": "hardhat verify",
    "deploy:verify:testnet": "hardhat run scripts/deploy.ts --network sapphireTestnet && echo \"After deploy: run 'pnpm --filter contracts -- run verify -- --network sapphireTestnet <CONTRACT_ADDRESS> <CONSTRUCTOR_ARGS>'\"",
    "audit:prepare": "mkdir -p flattened || true && hardhat flatten contracts/SquarexoMatch.sol > flattened/SquarexoMatch.sol",
    "check:repro": "node ./scripts/check_repro.js || echo 'Create scripts/check_repro.js to implement exact bytecode checks'"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@oasisprotocol/sapphire-paratime": "^2.3.0",
    "dotenv": "^17.4.1",
    "hardhat": "^2.26.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
